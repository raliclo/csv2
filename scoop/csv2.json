{
    "version": "0.1.1",
    "description": "Fail-loudly CSV parser and editor: names the record and field, never repairs",
    "homepage": "https://github.com/raliclo/csv2",
    "license": "MIT",
    "notes": [
        "Installed as csv2.exe. Verify with:  csv2 --version",
        "The archive is .tar.zst; Scoop unpacks it with the zstd it ships.",
        "Verified on 2026-09-17 by running this exact install on Windows 11: the hash",
        "checked ok, the shim resolves to apps\\csv2\\current, and `csv2 --version` prints",
        "csv2 0.1.1 (v0.1.1 / 4ae95f6) from bash, cmd.exe and zsh, and reads a CSV.",
        "If csv2 was placed by install.zsh before, run `./install.zsh --uninstall` first:",
        "Scoop overwrites that shim. After this, install.zsh refuses to write over it.",
        "Only 64bit is published: an archive is built on the machine that built the binary,",
        "and only that arrangement lets the version inside it be checked by running it."
    ],
    "architecture": {
        "64bit": {
            "url": "https://github.com/raliclo/csv2/releases/download/v0.1.1/csv2-0.1.1-windows-x86_64.tar.zst",
            "hash": "5d592628183405119eb8140d036fed99e88e575f2d892b9102221e46b2324c4b",
            "extract_dir": "csv2-0.1.1-windows-x86_64"
        }
    },
    "bin": "csv2.exe",
    "checkver": {
        "github": "https://github.com/raliclo/csv2"
    },
    "autoupdate": {
        "architecture": {
            "64bit": {
                "url": "https://github.com/raliclo/csv2/releases/download/v$version/csv2-$version-windows-x86_64.tar.zst"
            }
        },
        "hash": {
            "url": "$url.sha256"
        }
    }
}
