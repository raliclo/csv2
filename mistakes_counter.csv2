id,title,total,sameday_max,sameday_date,days_seen,first_seen,last_seen,corrective,shape,guard
編號,標題,總次數,單日最多,該日,發生天數,首次,最近,矯正措施,共同形狀,現行防範
1,測試量到的是環境或工具，不是被測物,6,3,2026-08-27,3,2026-08-20,2026-08-29,一個測試只在某個平台失敗（或只在某個平台通過）時，先問「它在那裡量的是不是同一樣東西」，再問「被測物有沒有平台差異」。順序反過來就會去改程式。測試不得依賴外部工具的輸出格式（diff 的 `<`/`>`、wc 的補空白）或某個工具的存在（stat、getent）；要比較就用 cmp，或在 zsh 裡逐行比。四個節點裡三個通過時，那三個是診斷的證據，不是把它打發掉的理由。,測試碰到的是環境，不是被測物。每一次程式在各平台行為都相同，不同的是量尺。它會往兩個方向壞：在輸出格式不同的平台上「失敗」（至少會叫），或在缺少某個工具的平台上「通過」（無聲，因為沒有人會再讀一個通過的案例）。第六次發生在「為了強制這條規則而寫的那個檢查」裡——`grep --include` 是 GNU 專屬的，guest 沒有它，於是那個檢查在那裡什麼都沒掃、並以空結果通過。,T218a 掃描每一支 .zsh 是否呼叫 stat(1)，檔案清單來自 zsh 的 glob 而不是 `grep --include`（那是 GNU 專屬的），並且在 glob 收到少於 4 個檔案時直接判失敗——零個檔案代表 glob 壞了，不代表樹是乾淨的。T218b 用一個確實呼叫 stat -c 的探針證明 T218a 會咬；那個配對在加入後的第一次跨平台執行就抓到了 T218a 自己的空洞通過。仍缺一般形式：這一條真正的樣子是「依賴外部工具的輸出格式或存在」，而 T218 只擋住 stat(1) 這一個具體形狀。
2,證據就在輸出裡，而我沒有讀它,2,1,2026-09-01,2,2026-08-29,2026-09-01,在「取一個新的識別碼／編號／名稱」之前，不是查一下就算了——要**讀那個查詢的輸出**。更好的做法是讓工具回答：T231 掃描測試檔，任何一個編號若出現在相距 100 行以上的兩處就判失敗；取新編號時直接問「最大的是幾號」，而不是假設下一個空號。,查詢跑了、輸出印在螢幕上、而下一步的動作與那個輸出矛盾。兩次都是測試編號撞號：2026-08-29 的 T214 與 Windows 節點那組案例撞號（那次是根本沒查）；2026-09-01 的 T219 與 -update-where 的案例撞號——那次 `grep -c 'T219'` 印出 10，就在螢幕上，而我把它當成一個不需要看的步驟跳過去了。編號當時早已到 229。,T231a 用「同一編號散落在相距 100 行以上的兩處」判定撞號——不是用出現次數（一個案例合理地會有多行 ok/bad，用次數判會有十幾個誤報）。在 1135 個案例上，唯一超過門檻的就是那個真正的撞號，零誤報。T231b 確認那次掃描真的讀到了檔案。
