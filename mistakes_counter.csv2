id,title,total,sameday_max,sameday_date,days_seen,first_seen,last_seen,corrective,shape,guard
編號,標題,總次數,單日最多,該日,發生天數,首次,最近,矯正措施,共同形狀,現行防範
1,測試碰到的不是被測物（環境、工具，或 fixture 弄丟了那個屬性）,8,3,2026-08-27,5,2026-08-20,2026-09-03,一個測試只在某個平台失敗（或只在某個平台通過）時，先問「它在那裡量的是不是同一樣東西」，再問「被測物有沒有平台差異」。順序反過來就會去改程式。測試不得依賴外部工具的輸出格式（diff 的 `<`/`>`、wc 的補空白）或某個工具的存在（stat、getent）；要比較就用 cmp，或在 zsh 裡逐行比。四個節點裡三個通過時，那三個是診斷的證據，不是把它打發掉的理由。,測試碰到的是環境，不是被測物。每一次程式在各平台行為都相同，不同的是量尺。它會往兩個方向壞：在輸出格式不同的平台上「失敗」（至少會叫），或在缺少某個工具的平台上「通過」（無聲，因為沒有人會再讀一個通過的案例）。第六次發生在「為了強制這條規則而寫的那個檢查」裡——`grep --include` 是 GNU 專屬的，guest 沒有它，於是那個檢查在那裡什麼都沒掃、並以空結果通過。,T218a 掃描每一支 .zsh 是否呼叫 stat(1)，檔案清單來自 zsh 的 glob 而不是 `grep --include`（那是 GNU 專屬的），並且在 glob 收到少於 4 個檔案時直接判失敗——零個檔案代表 glob 壞了，不代表樹是乾淨的。T218b 用一個確實呼叫 stat -c 的探針證明 T218a 會咬；那個配對在加入後的第一次跨平台執行就抓到了 T218a 自己的空洞通過。仍缺一般形式：這一條真正的樣子是「依賴外部工具的輸出格式或存在」，而 T218 只擋住 stat(1) 這一個具體形狀。
2,證據就在輸出裡，而我沒有讀它,5,3,2026-09-02,3,2026-08-29,2026-09-02,在「取一個新的識別碼／編號／名稱」之前，不是查一下就算了——要**讀那個查詢的輸出**。更好的做法是讓工具回答：T231 掃描測試檔，任何一個編號若出現在相距 100 行以上的兩處就判失敗；取新編號時直接問「最大的是幾號」，而不是假設下一個空號。,查詢跑了、輸出印在螢幕上、而下一步的動作與那個輸出矛盾。兩次都是測試編號撞號：2026-08-29 的 T214 與 Windows 節點那組案例撞號（那次是根本沒查）；2026-09-01 的 T219 與 -update-where 的案例撞號——那次 `grep -c 'T219'` 印出 10，就在螢幕上，而我把它當成一個不需要看的步驟跳過去了。編號當時早已到 229。,T231a 用「同一編號散落在相距 100 行以上的兩處」判定撞號——不是用出現次數（一個案例合理地會有多行 ok/bad，用次數判會有十幾個誤報）。在 1135 個案例上，唯一超過門檻的就是那個真正的撞號，零誤報。T231b 確認那次掃描真的讀到了檔案。
3,一條規則只套用到它成立範圍的一部分,10,5,2026-09-03,4,2026-09-01,2026-09-04,修好一處之後，先數同一個規則在這棵樹裡還有幾處（grep -c，而且不要接 head），再宣稱修好。而「數量對不對」的檢查要驗證的是對應關係，不是總和——兩個方向相反的錯誤會互相抵銷，讓檢查通過而兩個錯都留著。,一條規則被修好或被建立，但只套用到它成立範圍的一部分；剩下的那些沒有任何東西會指出來。七次：KU（Markdown 分隔列）、KV（--prefix，正確用法就在同一個函式裡）、KW（位元組相等，同一檔案的第二處）、KX（空行）、LC（寫死的略過數，而那一行上面的註解早就診斷出這個病）、T229（設了旗標沒有被計數，由新守衛自己找到）、以及 strerror 在 Windows 上（五個既有呼叫都在 #if 分支內，新加的那個不在，弄壞了 Windows 建置）。,T241a：每一個被設定的 Txxx_SKIPPED 旗標都必須被預期略過數消費，反之亦然；驗證的是對應關係而非總和。T241b 用一個真的含有未計數旗標的檔案證明它會咬。它在被寫出來的過程中就找到了 T229。只涵蓋這一個具體形狀，程式碼層面的重複仍缺一般形式。
4,說了要做，然後沒有做，而沒有任何東西會回報,5,5,2026-09-03,2,2026-09-02,2026-09-03,在使用者可見的文字裡說出「我會做 X」的當下就記下來（TaskCreate），不要等做完再回頭數。一個只存在於對話裡的承諾，與一個只存在於 session 逐字稿裡的缺陷是同一種東西——下一個讀者拿到的是一棵看起來完成了的樹。,說出口的意圖沒有任何機制追蹤，而其中一部分留在原地繼續誤導人。五件：AGENTS.md 沒改成「跑 dispatcher」（而它正在教人繞過它，那就是 KY 的成因）、KY 沒補上更正、fileKind 的 errno 沒改、T203 的假 skip 理由（我問了、沒得到答案、然後放掉了）、mistakes 第 2 條沒記。是使用者問「有沒有做」才發現的。,尚無強制檢查。依 skill 的判準這是單日集中而非跨日再犯，因此先固化做法：說出意圖的當下建立任務。若再跨一天發生，就需要一道真正的檢查。
